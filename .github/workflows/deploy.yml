      - name: Deploy to main branch (root)
        run: |
          cp -r out/* .
          # garante .nojekyll e CNAME SEMPRE após a cópia
          touch .nojekyll
          echo "www.studioarthub.com" > CNAME
          rm -rf out
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "🚀 Deploy automático via GitHub Actions (+.nojekyll +CNAME)" || echo "Nada para atualizar"
          git push origin main
